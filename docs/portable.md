# Portable-сборка FancyWM

## Готовый архив

[FancyWM-Portable-win-x64-20260914-210118-900.zip](../artifacts/portable/FancyWM-Portable-win-x64-20260914-210118-900.zip)

Путь: `E:\developing\fancywm\artifacts\portable\FancyWM-Portable-win-x64-20260914-210118-900.zip`.
Версия **2.19.1.14**, Release win-x64 со встроенным .NET; 510 файлов,
74 744 136 байт. Исходный commit:
`be19638b47b88c64bc08e949dc8366da62073d10` — включает исправление автозапуска
portable, регрессионные тесты и инструкцию в пакете.
Пометка README о рабочих изменениях относится к PERFORMANCE_STATUS.md и
PERFORMANCE_TODO.md; код приложения соответствует указанному commit.

SHA-256:

```text
A4A4B3F8858A2F3CA2114144903557D05DD2963BE5E465B8BABAEFED01201BA5
```

## Повторная упаковка

В Windows, с установленными Git, .NET SDK 10 и необходимыми компонентами
Windows SDK/WPF, выполните из корня репозитория:

```powershell
pwsh -File scripts/Build-Portable.ps1
```

Для другого каталога вывода, включая путь с пробелами:

```powershell
pwsh -File scripts/Build-Portable.ps1 -OutputRoot "E:\FancyWM test builds"
```

Скрипт определяет репозиторий по своему расположению, поэтому его также можно
вызвать по абсолютному пути из другого рабочего каталога. Он вызывает существующий
`Apply-DependencyPatches.ps1 -Mode Apply`: проверяет уже применённые патчи или
применяет их к ожидаемой чистой базе; конфликтующие изменения не перезаписывает.
Затем локально выполняет `dotnet publish` для GUI и CLI: Release win-x64,
self-contained, с restore, без single-file и trimming. Версии SDK, зависимостей
и gitlink скрипт не меняет.

Каждый запуск создаёт отдельную папку и ZIP с отметкой времени в
`artifacts/portable` (либо OutputRoot), README с версией/исходным commit и файл
`.zip.sha256`. Предыдущие пакеты не перезаписываются. README отмечает изменения
отслеживаемых файлов рабочего дерева, если они есть; для повторения сохранённого
исходного состояния собирайте из нужного commit. Применённые патчи зависимости
описывает существующий manifest.

## BAT и запуск

1. Распакуйте весь ZIP в отдельную папку.
2. Завершите другую копию FancyWM через значок в трее.
3. Запустите `FancyWM-GUI.exe`.

.NET Runtime включён. Настройки и журналы используют `%APPDATA%\FancyWM`.
`FancyWM.bat` запускает `"%~dp0FancyWM.exe" %*` и возвращает `%ERRORLEVEL%`.
Рабочий каталог менять не требуется; пути с пробелами поддерживаются.
CLI-вывод сохраняется. Для проверки без обычного UI доступны `--help`/`--version`.

BAT попадает в пакет через существующий `CopyToOutputDirectory=PreserveNewest`
в `FancyWM.csproj`; скрипт проверяет совпадение SHA-256 исходного и упакованного
файлов. Отдельной подмены BAT после сборки больше нет.

## Автозапуск portable

После распаковки и запуска `FancyWM-GUI.exe` включите автозапуск в общих
настройках. Это запуск при входе текущего пользователя в Windows. Создаётся
`FancyWM.lnk` в его папке Startup, с абсолютным путём к GUI текущей portable-папки
и соответствующим рабочим каталогом. Пробелы и Unicode в пути поддерживаются.
Отключение опции удаляет ярлык.

Ранее portable копировал встроенный ярлык `explorer.exe` с аргументом
`shell:appsFolder\2203VeselinKaraganev.FancyWM_9x2ndwrcmyd2c!App`, который
адресует Store-версию. Теперь неправильная ссылка не считается включённым
автозапуском, а включение опции заменяет её ссылкой на portable. Механизм
StartupTask для установленной Store-версии сохранён.

Храните распакованную папку по постоянному пути. После перемещения или обновления
в другую папку включите автозапуск из новой копии: ярлык будет обновлён.

## Проверка текущего пакета — 2026-09-14

- `scripts/Build-Portable.ps1` успешно опубликовал GUI и CLI в Release win-x64
  со встроенным .NET. Существующие предупреждения сборки сохранены в журнале.
- Полные наборы Release: `FancyWM.Tests` — **2183**, `FancyWM.Layouts.Tests` —
  **160**, `FancyWM.ThemeEngine.Tests` — **31**. Всего **2374** тестовых случая,
  без ошибок и пропусков; число подтверждено конечными результатами в TRX.
- ZIP полностью прочитан, CRC проверены; все 510 извлечённых файлов совпадают
  по SHA-256 с исходной папкой пакета. Хеш ZIP совпадает с `.zip.sha256`.
  Оба EXE, BAT и runtime присутствуют, BAT побайтно совпадает с исходником.
- Из распакованной папки с пробелами GUI, CLI и BAT выполнили `--help` и
  `--version` из другого рабочего каталога: все шесть запусков завершились с
  кодом 0, stdout/stderr совпадают. Версия в выводе — **2.19.1.14**.
- Патчи зависимости совпадают с manifest; gitlink сохранён. Код приложения
  после предыдущей проверки автозапуска не менялся. Обычный UI и регистрацию
  автозапуска при этой повторной упаковке не запускали.

Журнал сборки, проверка архива и результаты тестов сохранены локально в
`artifacts/portable-release-verification-20260914-210118/`, вне Git.

## Предыдущий пакет 2.19.1.13 — проверка автозапуска 2026-09-14

Архив `FancyWM-Portable-win-x64-20260914-203310-283.zip`, 74 744 148 байт,
исходный commit `9bfd3a2aa9062595fab8a6d2de2238534c727e4d`, SHA-256
`298EB3117EBFCE9FEE2515FD2A9144B8A266B7294C5F2CB2181E9C2BF13FF75E`.
Следующие результаты относятся к этому предыдущему пакету:

- Связанные тесты: **11 Debug + 11 Release**, без ошибок и пропусков. Включают
  шесть новых проверок настоящих Windows-ярлыков и пять проверок сохранения
  настроек, в том числе асинхронного чтения состояния автозапуска.
- ZIP полностью прочитан, CRC проверены, все 510 извлечённых файлов совпадают
  побайтно. SHA-256 совпадает с файлом `.zip.sha256`; оба EXE, BAT и встроенный
  runtime присутствуют. BAT совпадает с исходником.
- Из распакованного пути с пробелами GUI и CLI выполнили `--help`/`--version`
  с кодом 0 и одинаковым выводом. BAT `--version` из другого рабочего каталога
  также совпал с прямым CLI-запуском.
- Проверочный процесс с runtime готового пакета вызвал публичные
  `Autostart.EnableAsync/IsEnabledAsync/DisableAsync` из опубликованной DLL.
  В настоящей папке Startup пользователя проверены целевой GUI, рабочий каталог,
  пустые аргументы, состояние включения и удаление ярлыка.
- Windows запустила копию созданного ярлыка с аргументом `--version` из другого
  рабочего каталога; код завершения 0. Исходная запись автозагрузки и SHA-256
  пользовательских настроек после проверки сохранены. Пользовательский автозапуск
  не оставлен включённым проверкой; override StartupApproved для FancyWM отсутствовал.
- Патчи зависимостей проверены существующим механизмом. Версии зависимостей,
  SDK, gitlink и правила layout не менялись. `git diff --check` прошёл.

Это проверка регистрации и безопасного CLI-входа через Windows shell; реальная
перезагрузка/вход пользователя и интерактивное нажатие опции в UI не проводились.
Полная регрессия не повторялась. Существующие предупреждения сборки сохранены,
ошибок нет. Локальные результаты: `artifacts/portable-autostart-verification`, вне Git.

## Предыдущий пакет 2.19.1.9 — исторические результаты

Файл `artifacts/portable/FancyWM-Portable-win-x64-20260914-160522-654.zip`
при текущей проверке отсутствует; эти метаданные не означают доступность архива.
Исходный commit `2dc08a8d9a271f96fca07d5865c77c67d3685c72`,
74 742 542 байта, SHA-256
`0A2EDEF843820E9330368A2209FDFAF0CAD335629CA25363AD265E496196C8FE`.
Приведённые ниже результаты относятся к тому пакету, до исправления автозапуска.

- ZIP прочитан полностью: CRC и хеши всех извлечённых файлов совпадают.
- Проверены 669 ссылок на runtime/native/resource assets, наличие обоих
  deps/runtimeconfig, встроенного .NET и архитектура x64 EXE/runtime.
- Из распакованного пути с пробелами оба EXE выполнили `--help`/`--version`
  с кодом 0. Вывод GUI и CLI совпал.
- BAT запускался из другого рабочего каталога. Для `--help`, `--version`, их
  комбинации и дополнительных аргументов с пробелами/пустой строкой/спецсимволами
  stdout, stderr и код возврата совпали с прямым запуском FancyWM.exe.
- Отдельная тестовая консольная программа, рядом с неизменённой копией BAT,
  подтвердила точный массив аргументов (включая Unicode и путь с завершающим
  обратным слешем), сохранение stdout/stderr и передачу ненулевого кода **37**.
  Это проверка BAT; приложение FancyWM для получения ошибки не запускалось.
- Скрипт успешно повторно выполнен из другого рабочего каталога с OutputRoot,
  содержащим пробелы. Созданы новая папка/ZIP, исходный архив сохранён без изменений.
- Проверка существующих патчей прошла; версии зависимостей, SDK и gitlink не менялись.

Локальные журналы и результаты находятся в `artifacts/portable-verification`,
вне Git. Полная регрессия приложения и обычный UI не запускались: изменения
ограничены BAT, упаковкой и документацией. Существующие предупреждения сборки
сохранены в логах, ошибок нет. Внешняя публикация, установка и push не выполнялись.

## Статус и границы

Обе новые фичи и предыдущие оптимизации подтверждены пользователем на версии
2.19.1.7: «Все работает как надо». Это не подтверждает отдельно не заявленные
мониторы, DPI и приложения. Текущий этап касается автозапуска portable и упаковки;
правила layout не менялись, повторный интерактивный UI-проход не нужен.
Недоступные артефакты более ранних этапов не восстанавливаются и не предлагаются
как доступные для скачивания. Текущий ZIP создан локально; установка и публикация
архива в GitHub Releases не выполнялись. Исходники и документация предназначены
для синхронизации `main`, `feature/master-satellite-layout` и
`work/practical-closeout-20260913` с одноимёнными ветками `origin`.
